# References for section 1, opening slides (`10-` to `13a-`, `19z-`)

Not a slide (no leading digits, so the Makefile ignores it). The integrator should fold these
into `80-references.md` if they are wanted there.

The opening slides are standard textbook quantum mechanics and carry no research citation.
The one book reference worth listing, should the speaker want the new members to have a
follow-up text, is:

1. M. A. Nielsen and I. L. Chuang, *Quantum Computation and Quantum Information*,
   10th anniversary edition, Cambridge University Press (2010), chapter 2. Source for the
   state vector and its normalisation, the Born rule and post-measurement collapse, the
   density matrix $\rho=\ket{\psi}\bra{\psi}$, and the Pauli operators.

<!-- EDIT-FORWARD: this book is not filed in `1. raw/` and has no entry in
     `1. raw/references.bib`. Either drop it from `80-references.md` or have it filed with
     `aleph-ref` first. -->
